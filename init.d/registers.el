;; =============================================================================
;; Registers
;; =============================================================================

(set-register ?e (cons 'file dotemacs/path-to-dotemacs)) 
(set-register ?i (cons 'file dotemacs/path-to-init.d)) 
